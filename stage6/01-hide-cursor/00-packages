wtype

# добавить вместо unclutter

   /etc/xdg/labwc/rc.xml


   <keybind key="A-W-h">
      <action name="HideCursor" />
      <action name="WarpCursor" x="-1" y="-1" />
    </keybind>


    nano .config/labwc/autostart
    ~/autostart.sh

    autostart.sh
    swayidle timeout 5 'wtype -M alt -M logo h -m alt -m logo'
